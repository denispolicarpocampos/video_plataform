<div class="card">
  <div class="card-header">
    <h2>Video - <%= @video.name %></h2>
  </div>
  <div class="card-body">
    <h5 class="card-title">
      <%= link_to  "#{@video.url}", @video.url %>
    </h5>
  </div>
</div>